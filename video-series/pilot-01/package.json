{
  "name": "learn-pi-agent-01-explainer",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:assets": "node scripts/prepare-assets.mjs",
    "dev": "pnpm prepare:assets && npx --yes hyperframes@0.8.6 preview",
    "check": "pnpm prepare:assets && npx --yes hyperframes@0.8.6 check --at 6.5,24.5,51,71,79 --snapshots",
    "render": "pnpm prepare:assets && npx --yes hyperframes@0.8.6 render --quality high --output renders/learn-pi-agent-01-silent.mp4",
    "publish": "npx --yes hyperframes@0.8.6 publish"
  },
  "dependencies": {
    "gsap": "3.14.2"
  }
}
