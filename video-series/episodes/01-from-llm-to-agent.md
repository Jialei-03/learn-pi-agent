# Learn Pi Agent 01｜大模型如何一步步变成 Agent

- 对应课程：第 1 章
- 核心问题：一个能够回答问题的大模型，为什么还不能独立完成任务？
- 核心结论：Agent 的行动能力来自模型之外的 Runtime；Harness 再把 Runtime、工具、会话、环境和界面装配成完整系统。
- 无声分镜基准：80 秒；真人录音后按自然语速重定时

## 分镜

| 时间 | 画面 | 动画重点 | 口播 |
| --- | --- | --- | --- |
| 00–08s | 封面：Mox 与“回答问题 ≠ 完成任务” | 问号先出现，任务清单随后展开 | ① |
| 08–18s | 一次模型调用 | 输入、模型、响应依次连线 | ② |
| 18–29s | 模型与宿主边界 | Tool Call 停在边界，宿主一侧亮起 | ③ |
| 29–41s | Agent Loop | 指针沿“调用—执行—写回”走一圈 | ④ |
| 41–52s | Message、Context、State、Session | 四层容器从内到外展开 | ⑤ |
| 52–63s | 停止信号与运行决定 | 左侧信号出现，右侧 Runtime 选择分支 | ⑥ |
| 63–73s | Runtime 与 Harness | Runtime 外围逐个装上工具、会话和策略 | ⑦ |
| 73–80s | 总结 | 三行结论依次落下，Mox 眨眼 | ⑧ |

## 连续口播稿

① 大模型很会回答问题。但查资料、用工具、保存进度，再继续判断——一次回答还不够。

② 一次模型调用很简单：宿主准备输入，模型生成响应，宿主接收结果。响应回来，这次调用就结束了。

③ 模型写出 Tool Call，动作还没有发生。它只是在提出结构化请求；真正执行工具的，是模型外面的宿主程序。

④ Agent 的关键，是把单次调用变成循环：模型提出 Tool Call，Runtime 校验并执行工具，把 Tool Result 写回 Context，再让模型继续判断，直到得到最终回答。

⑤ 这里有四个不同对象：Message 是一条记录；Context 是本轮给模型的输入快照；State 是运行中的完整数据；Session 保存跨越多轮的历史。

⑥ 模型只能返回 stop reason。整段运行是否继续，由 Runtime 结合响应内容、错误、取消、超时和轮次上限来决定。

⑦ Harness 再把模型连接、Runtime、工具、Session、执行环境和界面装配到一起。我们看到的 Agent 产品，通常是这套完整系统。

⑧ 所以，Agent 不是更聪明的一次回答，而是一段被程序持续推动的运行。下一章，我们看不同模型 API 怎样表达同一条消息链。

## 事实锚点

- Tool Call 是模型输出中的动作请求，不是动作已经执行的证据。
- `stopReason` 描述一次模型生成怎样结束，不自动等于整个 Agent Run 已结束。
- Context 是给本次模型调用的投影，不等于完整 State 或 Session。
