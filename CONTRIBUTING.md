# 参与 Learn Pi Agent

感谢你愿意帮助这个中文学习仓库变得更准确、更容易理解。贡献不只意味着增加内容：指出一句让初学者误解的话、补上缺失的版本条件、修正一个源码链接，同样很重要。

## 可以贡献什么

- 指出概念解释、API 字段或源码职责中的错误；
- 补充更小、更直观的例子或 SVG 图解；
- 把模糊的行业热词改写成可验证的问题；
- 为 Pi 的新版本记录路径或行为变化；
- 补充与具体工程问题直接相关的论文原文；
- 修复错别字、失效链接、术语不一致与无障碍说明。

如果准备写较长章节，建议先通过 Issue 说明它解决的问题、面向读者和计划引用的 Pi 版本，避免和正在进行的内容重复。

## 章节没有固定模板

本仓库刻意不要求每章套用相同小节。不同类型的内容应该选择适合自己的自然结构：

- **概念章节**：适合从具体问题、反例和最小模型开始；
- **API 对比**：适合用同一任务的请求/响应结构和语义映射表；
- **源码解析**：适合从类型、状态所有者与调用关系进入；
- **运行追踪**：适合按输入、事件、状态变化和输出组织；
- **论文背景**：适合说明研究问题、主要证据、局限及与 Pi 的关系；
- **参考手册**：适合稳定定义、索引和交叉链接。

虽然结构不同，新章节开头都应让读者在一分钟内知道：**这一章解决什么问题，读完能够解释或完成什么。**

## 写作原则

### 正文只负责讲解

面向读者的章节不布置练习、作业、自测或思考题，也不要求读者回答、记录、画图或复述。可以用问题引出概念，但必须紧接着给出解释；章节结尾只做知识总结和下一章衔接。

### 先讲清楚，再引入术语

首次出现的核心术语同时给出中英文，并链接[术语表](references/glossary.md)或就地解释。不要连续堆叠没有定义的热词，也不要用一个更模糊的词解释另一个词。

### 把事实、官方说明、研究结论和推断分开

建议使用清楚的信号：

- “在固定 commit 的 `agent-loop.ts` 中……”表示可定位的源码事实；
- “OpenAI 官方文档目前规定……”表示带核对日期的协议说明；
- “论文在其评测设置中报告……”表示受实验条件限制的研究结论；
- “可以把它近似理解为……”表示教学类比；
- “由此推断……”表示作者解释，而不是上游原话。

不要把合理推断包装成维护者设计意图。若要声称“Pi 为了 X 所以这样设计”，应提供维护者文档、RFC、提交说明或其他直接证据；否则改写为“这种分层带来的一个效果是 X”。

### 图要减少认知负担

SVG 或 HTML 图解只在关系、顺序、状态变化很难用短段落表达时使用。每张图应：

- 有明确标题和描述性替代文本；
- 节点数量控制在当前问题所需范围；
- 用文字或形状同时表达关键差异，不只依赖颜色；
- 标注箭头含义，避免把教学主线误认为完整依赖图；
- 在常见宽度下渲染检查，确保中文不溢出。

## 来源与版本规则

### Pi 源码

Pi 源码链接必须固定到 [版本基线](references/version-baseline.md)记录的 commit：

```text
086c32e74530564922d011ade23ff582c9d63116
```

不要在讲解正文中使用指向 `blob/main` 或 `tree/main` 的源码链接。若需要升级基线：

1. 先记录新旧 commit 和核对日期；
2. 验证关键类型、Agent Loop 与 coding-agent 主路径；
3. 更新版本记录和源码地图；
4. 再更新受影响章节，并说明行为差异。

项目首页或最新文档可以链接默认入口，但源码证据必须可复现。

### 模型 API 与协议

当前 API 只引用供应商或标准组织的官方文档，并在文章或版本表中标注核对日期。博客、问答和 SDK 示例可以帮助发现问题，不能替代最终协议证据。

API 对比应先对齐语义，再比较字段。例如先说明 Tool Call 与 Tool Result 怎样关联，再展示 OpenAI 和 Anthropic 的字段；不要因为两个字段名字相似就断言行为完全相同。

### 论文

论文优先链接 arXiv、会议正式页面或作者发布的原文，不用二手摘要替代论文。引用时至少说明：

- 它研究了什么问题；
- 为什么与当前 Pi 章节有关；
- 这个联系是背景、对照，还是能在源码中验证的直接实现；
- 不应被扩张到哪些论文没有评测的场景。

避免把“热门”“经典”当作结论正确的证据。

## 安全、隐私与版权

提交中不接受：

- API Key、访问 Token、Cookie、浏览器会话或认证头；
- 浏览器用户数据、账号信息或私有服务地址；
- 指向个人电脑的本地绝对路径；
- 含真实凭据形状、可能被误复制使用的示例；
- 大段无许可转载的文档、论文、课程或仓库内容。

示例请使用明显的占位值和虚构数据。引用外部内容时以概括和链接为主，短引文要注明来源并遵守许可与合理引用边界。

涉及命令执行、文件修改、网络或凭据的章节，必须区分“模型请求”和“宿主执行”，并提醒读者检查权限、沙箱与副作用。

## Issue 与 Pull Request

Issue 和 PR 标题建议使用中文，代码标识、API 字段、包名和函数名保留原始英文。标题可以采用：

```text
文档：解释 tool result 与 tool call 的关联
修正：更新 agent-loop 固定源码链接
讨论：如何组织 compaction 章节
```

PR 描述请包含：

1. 这次修改解决什么读者问题；
2. 影响哪些章节或图；
3. 使用的 Pi commit 与外部文档核对日期；
4. 哪些陈述已由源码或官方文档验证；
5. 图解是否完成渲染检查。

## 提交前自查

- [ ] 开头明确了问题与学习结果；
- [ ] 首次出现的术语已有解释；
- [ ] Pi 源码链接固定到同一 commit；
- [ ] API 链接来自官方文档并有核对日期；
- [ ] 论文链接到原文，并说明与 Pi 的具体关系；
- [ ] 教学类比没有写成严格定义；
- [ ] 内部 Markdown 链接可达；
- [ ] SVG 已检查中文、箭头、颜色与替代文本；
- [ ] 不含凭据、本地隐私路径或大段转载；
- [ ] 没有把后续计划写成空链接。

## 一个好贡献的判断标准

读者不一定因为页面更长而理解得更深。好的贡献应该让至少一件事变得更可判断：一个概念能被复述，一条调用链能被定位，一个版本差异能被复现，或者一个原本隐藏的边界能被看见。
